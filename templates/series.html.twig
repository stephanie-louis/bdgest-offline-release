{% extends 'base.html.twig' %}

 {% block body %}
<h1>Séries ({{ total }})</h1>

<div class="container">
    <div class="row d-flex " style="justify-content: center;">
        {% for char in letters %}
            <div class="m-2">
            {% if letter == char %}
                <strong>{{ char|upper }}</strong>
            {% else %}
                <a href="/series/{{ char }}">{{ char|upper }}</a>
            {% endif %}
            </div>
            <div class="m-1">|</div>
        {% endfor %}
    </div>
    <div class="row mt-3">
    {% for serie in series %}
        <div class="card col-sm-6 col-md-3 col-lg-2 mb-3 border-0">
{#            <img class="card-img-top" src="/images/serie/planche/{{ album['img_planche'] | fileFromURL }}" alt="{{ serie.titre }}"#}
{#                 data-toggle="tooltip" data-placement="bottom" data-html="true"#}
{#                 title="<p>{{ serie.titre }}</p>"#}
{#            />#}
            <img class="card-img-top" src="{{ serie.imgPlanche }}" alt="{{ serie.titre }}" />
            <div class="card-body">
                <p class="card-text">{{ serie.titre }}</p>
                <a href="/albums/{{ serie.titre|first|lower }}" class="btn btn-outline-primary">
                    <i class="fa fa-book" aria-hidden="true"></i> Voir les albums
                </a>
            </div>
        </div>

    {% endfor %}
    </div>

    <div class="row d-flex " style="justify-content: center;">
        {% for char in letters %}
            <div class="m-2">
                {% if letter == char %}
                    <strong>{{ char|upper }}</strong>
                {% else %}
                    <a href="/series/{{ char }}">{{ char|upper }}</a>
                {% endif %}
            </div>
            <div class="m-1">|</div>
        {% endfor %}
    </div>
</div>

{% endblock %}